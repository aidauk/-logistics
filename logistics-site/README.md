# Сайт-визитка логистической компании

Статический сайт для презентации услуг грузоперевозок из Китая в Узбекистан.

## 🚀 Быстрый старт

### Установка и запуск локально

```bash
npm install
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

### Сборка для продакшена

```bash
npm run build
```

Статические файлы будут в папке `out/`.

## 📝 Как изменить контент

### Изменение тарифов калькулятора

Откройте файл `lib/calculator.ts` и измените значения в объекте `tariffs`:

```typescript
export const tariffs: Record<DeliveryType, Tariff> = {
  auto: {
    pricePerKg: 2.5, // Измените цену за килограмм
    deliveryDays: 15, // Измените срок доставки
    minWeight: 100,   // Минимальный вес
  },
  // ...
};
```

### Изменение SEO meta-тегов

Откройте файл `lib/seo.ts` и измените данные в объекте `seoConfig`:

```typescript
export const seoConfig: Record<string, SEOData> = {
  home: {
    title: "Ваш заголовок",
    description: "Ваше описание",
    keywords: "ключевые, слова",
    // ...
  },
  // ...
};
```

### Добавление новых городов

В файле `lib/calculator.ts` добавьте города в массив `cities`:

```typescript
export const cities: City[] = [
  { id: "new-city", name: "Новый город", country: "Китай" },
  // ...
];
```

## 🌐 Деплой на GitLab Pages

### 1. Подготовка проекта

Убедитесь, что в `next.config.mjs` указан правильный `basePath` (если сайт будет в подпапке):

```javascript
basePath: process.env.BASE_PATH || "",
```

### 2. Сборка статического сайта

```bash
npm run build
```

### 3. Настройка GitLab CI/CD

Создайте файл `.gitlab-ci.yml` в корне проекта:

```yaml
image: node:20

pages:
  stage: deploy
  script:
    - npm install
    - npm run build
  artifacts:
    paths:
      - out
  only:
    - main
```

### 4. Публикация

1. Закоммитьте и запушьте код в GitLab
2. GitLab автоматически соберёт проект и опубликует его на Pages
3. Сайт будет доступен по адресу: `https://<username>.gitlab.io/<project-name>`

## 📁 Структура проекта

```
logistics-site/
├── app/              # Страницы Next.js
│   ├── page.tsx      # Главная страница
│   ├── services/     # Страница услуг
│   ├── calculator/   # Калькулятор
│   └── contacts/     # Контакты
├── components/        # React компоненты
├── lib/              # Утилиты и конфигурация
│   ├── seo.ts        # SEO настройки
│   └── calculator.ts # Логика калькулятора
└── styles/           # Стили
```

## 🎨 Настройка дизайна

Цвета и стили настраиваются в:
- `tailwind.config.ts` - цветовая схема
- `app/globals.css` - глобальные стили и классы

## 📞 Контакты в коде

Измените контактную информацию в:
- `components/Footer.tsx` - подвал сайта
- `app/contacts/page.tsx` - страница контактов

## ⚙️ Технические детали

- **Framework**: Next.js 14 (App Router)
- **Стили**: Tailwind CSS
- **Язык**: TypeScript
- **Экспорт**: Статический (без сервера)
- **Адаптивность**: Desktop / Tablet / Mobile

## 📄 Лицензия

Приватный проект.




