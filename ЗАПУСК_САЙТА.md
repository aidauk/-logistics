# Инструкция по запуску сайта-визитки локально

## 📋 Требования

- Node.js версии 18 или выше
- npm (устанавливается вместе с Node.js)

## 🚀 Быстрый запуск

### Шаг 1: Перейти в папку проекта

```bash
cd logistics-site
```

### Шаг 2: Установить зависимости (если еще не установлены)

```bash
npm install
```

> **Примечание:** Если папка `node_modules` уже существует, этот шаг можно пропустить.

### Шаг 3: Запустить сайт в режиме разработки

```bash
npm run dev
```

### Шаг 4: Открыть в браузере

Откройте в браузере адрес: **http://localhost:3000**

---

## 📝 Дополнительные команды

### Остановка сервера

Нажмите `Ctrl + C` в терминале, где запущен сервер.

### Сборка для продакшена

```bash
npm run build
```

### Запуск собранной версии

```bash
npm run start
```

### Проверка кода (линтинг)

```bash
npm run lint
```

---

## 🔧 Решение проблем

### Порт 3000 уже занят

Если порт 3000 занят, Next.js автоматически предложит использовать следующий свободный порт (3001, 3002 и т.д.).

Или можно указать другой порт вручную:

```bash
npm run dev -- -p 3001
```

### Ошибки при установке зависимостей

1. Удалите папку `node_modules` и файл `package-lock.json`:
   ```bash
   rm -rf node_modules package-lock.json
   ```

2. Установите зависимости заново:
   ```bash
   npm install
   ```

### Проблемы с TypeScript

Убедитесь, что установлена правильная версия TypeScript:
```bash
npm install typescript@^5.0.0 --save-dev
```

---

## 📁 Структура сайта

- `app/page.tsx` - Главная страница
- `app/services/page.tsx` - Страница услуг
- `app/calculator/page.tsx` - Калькулятор стоимости доставки
- `app/contacts/page.tsx` - Контакты
- `components/Header.tsx` - Шапка сайта
- `components/Footer.tsx` - Подвал сайта
- `lib/calculator.ts` - Логика калькулятора
- `lib/seo.ts` - SEO настройки

---

## 🎨 Изменение контента

### Тарифы калькулятора

Откройте файл `lib/calculator.ts` и измените значения в объекте `tariffs`.

### Контактная информация

Откройте файлы:
- `components/Footer.tsx` - для изменения контактов в подвале
- `app/contacts/page.tsx` - для изменения страницы контактов

### SEO настройки

Откройте файл `lib/seo.ts` и измените данные в объекте `seoConfig`.

---

## ✅ Проверка работы

После запуска сайта убедитесь, что:

1. ✅ Сайт открывается в браузере
2. ✅ Навигация работает (переходы между страницами)
3. ✅ Калькулятор работает корректно
4. ✅ Сайт адаптивный (проверьте на разных размерах экрана)

---

## 📞 Поддержка

Если возникли проблемы, проверьте:
- Версию Node.js: `node --version` (должна быть 18+)
- Версию npm: `npm --version`
- Консоль браузера на наличие ошибок (F12)
- Терминал на наличие ошибок


