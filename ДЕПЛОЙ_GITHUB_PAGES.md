# Инструкция по деплою на GitHub Pages

## 📋 Предварительные требования

1. Аккаунт на GitHub
2. Репозиторий на GitHub (если еще не создан, создайте новый)
3. Git установлен на вашем компьютере

## 🚀 Вариант 1: Деплой через GitHub Actions (Рекомендуется)

Этот вариант автоматически собирает и деплоит сайт при каждом push в основную ветку.

### Шаг 1: Подготовка репозитория

1. Если у вас еще нет репозитория на GitHub, создайте его:
   - Перейдите на https://github.com/new
   - Введите название репозитория (например: `logistics-site` или `username.github.io`)
   - Выберите публичный или приватный репозиторий
   - Нажмите "Create repository"

2. Если репозиторий уже существует, перейдите к следующему шагу.

### Шаг 2: Настройка GitHub Pages

1. Перейдите в настройки репозитория: `Settings` → `Pages`
2. В разделе **Source** выберите:
   - **Source**: `GitHub Actions`
3. Сохраните изменения

### Шаг 3: Загрузка кода на GitHub

Если код еще не на GitHub:

```bash
# Перейдите в корневую папку проекта
cd C:\Users\ASUS-PC\Desktop\Dachilla-uz-master

# Инициализируйте git (если еще не инициализирован)
git init

# Добавьте удаленный репозиторий (замените YOUR_USERNAME и REPO_NAME)
git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git

# Добавьте все файлы
git add .

# Создайте коммит
git commit -m "Initial commit: Logistics site"

# Отправьте в репозиторий (если репозиторий новый, используйте -u)
git branch -M main
git push -u origin main
```

Если код уже на GitHub, просто убедитесь, что файл `.github/workflows/deploy-logistics-site.yml` добавлен:

```bash
git add .github/workflows/deploy-logistics-site.yml
git commit -m "Add GitHub Actions workflow for deployment"
git push
```

### Шаг 4: Проверка деплоя

1. Перейдите на вкладку **Actions** в вашем репозитории
2. Дождитесь завершения workflow (обычно 2-3 минуты)
3. После успешного деплоя сайт будет доступен по адресу:
   - Если репозиторий называется `username.github.io`: `https://username.github.io`
   - Если репозиторий имеет другое название: `https://username.github.io/repo-name`

### Шаг 5: Настройка базового пути (если репозиторий не в корне)

Если ваш сайт находится в подпапке (например, `username.github.io/repo-name`), workflow автоматически настроит базовый путь.

Если нужно настроить вручную, отредактируйте `logistics-site/next.config.mjs`:

```javascript
basePath: process.env.BASE_PATH || "/repo-name",  // Замените repo-name на название вашего репозитория
```

## 🔄 Обновление сайта

После каждого изменения кода просто сделайте commit и push:

```bash
git add .
git commit -m "Описание изменений"
git push
```

GitHub Actions автоматически пересоберет и задеплоит сайт!

---

## 🛠️ Вариант 2: Ручной деплой (Альтернатива)

Если вы хотите задеплоить вручную:

### Шаг 1: Сборка проекта

```bash
cd logistics-site
npm install
npm run build
```

### Шаг 2: Настройка Git для деплоя

```bash
# Перейдите в папку out (результат сборки)
cd out

# Инициализируйте git
git init

# Добавьте remote (замените на ваш репозиторий)
git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git

# Добавьте файлы
git add .

# Создайте коммит
git commit -m "Deploy site"

# Отправьте в ветку gh-pages
git branch -M gh-pages
git push -u origin gh-pages
```

### Шаг 3: Настройка GitHub Pages

1. Перейдите в `Settings` → `Pages`
2. В разделе **Source** выберите:
   - **Branch**: `gh-pages`
   - **Folder**: `/ (root)`
3. Сохраните изменения

---

## ⚙️ Настройка домена (опционально)

Если у вас есть собственный домен:

1. В настройках репозитория (`Settings` → `Pages`) укажите ваш домен
2. Добавьте файл `CNAME` в папку `logistics-site/public/` с вашим доменом:
   ```
   example.com
   ```
3. Настройте DNS записи у вашего провайдера домена

---

## 🔍 Решение проблем

### Сайт не обновляется после деплоя

- Очистите кэш браузера (Ctrl+Shift+R или Cmd+Shift+R)
- Проверьте логи в GitHub Actions
- Убедитесь, что workflow завершился успешно

### Ошибка "404 Not Found"

- Проверьте, правильно ли настроен `basePath` в `next.config.mjs`
- Убедитесь, что в GitHub Pages выбран правильный источник (GitHub Actions или branch)

### Ошибки при сборке

- Проверьте логи в GitHub Actions
- Убедитесь, что все зависимости указаны в `package.json`
- Проверьте, что Node.js версии 18+ установлена в workflow

### Изображения не загружаются

- Убедитесь, что `images.unoptimized: true` установлено в `next.config.mjs`
- Проверьте пути к изображениям (они должны быть относительными)

---

## 📝 Полезные ссылки

- [Документация GitHub Pages](https://docs.github.com/en/pages)
- [Документация Next.js - Static Export](https://nextjs.org/docs/app/building-your-application/deploying/static-exports)
- [GitHub Actions для Pages](https://github.com/actions/deploy-pages)

---

## ✅ Чек-лист перед деплоем

- [ ] Код загружен в GitHub репозиторий
- [ ] GitHub Actions workflow добавлен (`.github/workflows/deploy-logistics-site.yml`)
- [ ] GitHub Pages настроен в Settings → Pages (источник: GitHub Actions)
- [ ] Все зависимости установлены (`npm install` выполнен)
- [ ] Проект собирается без ошибок (`npm run build` успешен)
- [ ] Контактная информация обновлена (телефон, email в Header)
- [ ] Все ссылки работают корректно

---

Удачного деплоя! 🚀

